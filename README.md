# IdeaSphere

Idea sharing platform. See backend and frontend folders.

Quick start:

1. Backend

```bash
cd backend
cp .env.example .env
# edit .env with your MongoDB URI and JWT_SECRET
npm install
npm run dev
```

2. Frontend

```bash
cd frontend
cp .env.example .env
npm install
npm start
```
